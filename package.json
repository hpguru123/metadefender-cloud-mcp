{
  "name": "metadefender-cloud-mcp",
  "version": "0.1.1",
  "description": "MCP server for the OPSWAT MetaDefender Cloud API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node --test test/*.test.mjs",
    "build:extension": "npm run build && esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=extension/server/index.js --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\" && mcpb pack extension metadefender-cloud.mcpb"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^26.6.2",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "type": "module",
  "bin": {
    "metadefender-cloud-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
